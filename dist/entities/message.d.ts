import { type WAMessage, type WASocket } from '@whiskeysockets/baileys';
import type { RateLimiter } from '../connection/rate-limiter.js';
import type { Jid, MediaSource, MessageId, SendTextOptions } from '../types/common.js';
export declare class Message {
    private readonly socket;
    private readonly raw;
    private readonly rateLimiter?;
    private readonly sender;
    constructor(socket: WASocket, raw: WAMessage, rateLimiter?: RateLimiter | undefined);
    get id(): MessageId;
    get chatId(): Jid;
    get senderId(): Jid;
    get fromMe(): boolean;
    get isGroup(): boolean;
    get text(): string;
    private get activeContextInfo();
    get mentions(): Jid[];
    get quoted(): Message | null;
    get isMedia(): boolean;
    get timestamp(): Date;
    reply(text: string, options?: SendTextOptions): Promise<Message>;
    replyWithImage(source: MediaSource, caption?: string): Promise<void>;
    replyWithVideo(source: MediaSource, caption?: string): Promise<void>;
    replyWithAudio(source: MediaSource, asVoiceNote?: boolean): Promise<void>;
    replyWithDocument(source: MediaSource, fileName?: string, caption?: string): Promise<void>;
    react(emoji: string): Promise<void>;
    edit(newText: string): Promise<void>;
    delete(forEveryone?: boolean): Promise<void>;
    forward(to: Jid): Promise<void>;
    pin(duration?: 86_400 | 604_800 | 2_592_000): Promise<void>;
    unpin(): Promise<void>;
    downloadMedia(): Promise<Buffer>;
}
